const R = 20,
	  H = 30,
      S = Math.PI * Math.pow(R, 2),
      str = `**************
Обьем цилиндра с площадью основы S = ${S}, радиусом R = ${R} и высотой H = ${H} равен:
--------------------
V = ${S * H}
-------------------
end.
`;

console.log(str);
